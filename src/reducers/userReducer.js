// handle all actions pertaining to the user (login, registration, ...)
const user = (state = { testMessage: "Hello Team Thanos" }, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default user;
